export interface Employee {
  id: number;
  firstName: string;
  lastName: string;
  email: string;
  position: string;
  photoUrl: string;
  status: 'active' | 'inactive';
  phone: string;
  birthDate: string;
}

